import Vue from "vue";
import Vuetify from "vuetify/lib";

Vue.use(Vuetify);

export default new Vuetify({
  theme: {
    primary: "#9652ff",
    success: "#3cd1c2",
    info: "#ffa2c",
    error: "#f83e70",
  },
});
